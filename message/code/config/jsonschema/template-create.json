{
	"type": "object",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "properties": {
        "name": {"type":"string"},
        "lang": {"type":"string"},
        "type": {"type":"string", "enum": ["email", "phone"] },
        "subject": {"type":"string"},
        "context": {"type":"string"},
        "headers": {
            "type":"object"
        },
        "tags": {
            "type": "array"
        }
    },
    "required": ["name", "lang", "type", "subject", "context"],
    "additionalProperties": false
}